---
title: Get Database Certificate
sidebarTitle: Get Certificate
api: GET https://api.vertracloud.app/v1/databases/{id}/credentials/certificate
authMethod: bearer
description: Retrieves the certificate for a specific database by its ID.
---

{/* @see binary (certificate bundle) — @vertracloud/api-types */}
<ParamField path="id" type="string" required>
  The ID of the database to retrieve the certificate for.
</ParamField>

### Response

The response is a JSON object containing three PEM-encoded certificate components for TLS connection to the database.

<ResponseField name="response" type="object">
  The certificate details for secure database connections.

  <Expandable title="Toggle object">

  <ResponseField name="crt" type="string">
    The client certificate. Use it with `key` for TLS client authentication.
  </ResponseField>

  <ResponseField name="key" type="string">
    The private key for the client certificate. Use it with `crt` for TLS client authentication.
  </ResponseField>

  <ResponseField name="pem" type="string">
    The CA certificate (certification authority). Use this to verify the server's certificate chain when establishing a TLS connection.
  </ResponseField>

  </Expandable>
</ResponseField>

<ResponseExample>

```json Response
{
  "response": {
    "crt": "-----BEGIN CERTIFICATE-----\nMIID1TCCAr2gAwIBAgIUe7z...\n-----END CERTIFICATE-----",
    "key": "-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkqhkiG9w0BAQE...\n-----END PRIVATE KEY-----",
    "pem": "-----BEGIN CERTIFICATE-----\nMIIDazCCAlOgAwIBAgIUT...\n-----END CERTIFICATE-----"
  }
}
```

</ResponseExample>

### Usage

Every engine requires TLS **and** the client certificate. Save each field to a file:

| Field | Save as | Contains |
|---|---|---|
| `crt` | `certificate.crt` | Client certificate |
| `key` | `certificate.key` | Client private key |
| `pem` | `certificate.pem` | Client key + client certificate + the CA that signs the server |

Use `certificate.pem` as the CA file (and, for MongoDB, as the certificate-key file), and
`certificate.crt` + `certificate.key` as the client certificate. The port is unique per database —
read it from [Get Database](/api-reference/endpoint/databases/get). Commands and driver examples for
each engine are in [How do I connect to each engine?](/databases#how-do-i-connect-to-each-engine).

### Error Responses

<ResponseField name="code" type="string">
  The error code indicating the reason for failure. Possible values:
  - `UNAUTHORIZED`: The user is not authenticated or lacks sufficient permissions.
  - `DATABASE_NOT_FOUND_OR_FORBIDDEN`: The specified database does not exist or the user lacks access.
  - `FAILED_TO_GET_CERTIFICATE`: Failed to retrieve the certificate.
</ResponseField>

<ResponseField name="message" type="string">
  A descriptive message providing additional details about the error.
</ResponseField>

<ResponseExample>

```json Error Response (403)
{
  "code": "UNAUTHORIZED"
}
```

```json Error Response (404)
{
  "code": "DATABASE_NOT_FOUND_OR_FORBIDDEN"
}
```

```json Error Response (500)
{
  "code": "FAILED_TO_GET_CERTIFICATE"
}
```

</ResponseExample>
