---
title: Restart Application
sidebarTitle: Restart Application
api: POST https://api.vertracloud.app/v1/apps/{id}/restart
authMethod: bearer
description: Restarts an application by its ID.
---

{/* @see RESTPostAPIApplicationStartResponse — @vertracloud/api-types */}

<ParamField path="id" type="string" required>
  The application ID to restart. If the application
  belongs to a workspace, include the workspace ID in the format `appId-workspaceId`.
</ParamField>

### Response

<ResponseField name="response" type="object">
  The response containing the operation status.

  <Expandable title="Toggle object">

  <ResponseField name="status" type="string">
    The operation status (`restarting`).
  </ResponseField>

  </Expandable>
</ResponseField>

<ResponseExample>

```json Response
{
  "response": {
    "status": "restarting"
  }
}
```

</ResponseExample>
