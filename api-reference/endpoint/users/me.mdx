---
title: User Information
sidebarTitle: User Info
api: GET https://api.vertracloud.app/v1/users/me
authMethod: bearer
description: Returns complete user information including applications, databases, plan and OAuth connections.
---

{/* @see APIUserInfoResponse — @vertracloud/api-types */}
### Response

<ResponseField name="response" type="APIUserInfoResponse">
  Complete authenticated user data.

  <Expandable title="Toggle object">

  <ResponseField name="id" type="string">
    Unique user ID (Snowflake).
  </ResponseField>

  <ResponseField name="email" type="string">
    User email.
  </ResponseField>

  <ResponseField name="name" type="string">
    User display name.
  </ResponseField>

  <ResponseField name="plan_id" type="integer">
    Numeric plan ID. Values: 1=FREE, 2=ECONOMY, 3=PRO, 4=SCALE, 9=INTERMEDIARY, 5=ENTERPRISE_4, 19=ENTERPRISE_6, 6=ENTERPRISE_8, 10=ENTERPRISE_12, 11=ENTERPRISE_14, 7=ENTERPRISE_16, 12=ENTERPRISE_18, 13=ENTERPRISE_20, 14=ENTERPRISE_22, 15=ENTERPRISE_24, 16=ENTERPRISE_26, 17=ENTERPRISE_28, 18=ENTERPRISE_30, 8=ENTERPRISE_32.
  </ResponseField>

  <ResponseField name="language" type="string">
    User's preferred language (`pt-br`, `en-us`, `es-es`).
  </ResponseField>

  <ResponseField name="created_at" type="string" format="date-time">
    Account creation date (ISO 8601).
  </ResponseField>

  <ResponseField name="updated_at" type="string" format="date-time">
    Account last update date (ISO 8601).
  </ResponseField>

  <ResponseField name="plan" type="APIUserPlan">
    Current plan information.

    <Expandable title="Toggle object">

    <ResponseField name="id" type="integer">
      Plan ID.
    </ResponseField>

    <ResponseField name="name" type="string">
      Plan name (e.g., `free`, `pro`, `scale`).
    </ResponseField>

    <ResponseField name="expires_at" type="string | null" format="date-time">
      Plan expiration date. `null` for plans without expiration.
    </ResponseField>

    <ResponseField name="duration" type="integer">
      Plan duration in days.
    </ResponseField>

    <ResponseField name="memory" type="APIUserPlanMemory">
      Plan memory usage.

      <Expandable title="Toggle object">

      <ResponseField name="limit" type="integer">
        Memory limit in MB.
      </ResponseField>

      <ResponseField name="used" type="integer">
        Memory currently in use in MB.
      </ResponseField>

      </Expandable>
    </ResponseField>

    </Expandable>
  </ResponseField>

  <ResponseField name="applications" type="APIApplication[]">
    List of user applications.

    <Expandable title="Toggle object">

    <ResponseField name="id" type="string">
      Unique application ID (Snowflake).
    </ResponseField>

    <ResponseField name="cluster" type="integer">
      ID of the cluster hosting the application.
    </ResponseField>

    <ResponseField name="type" type="integer">
      Application type. `1` = Bot, `2` = Website.
    </ResponseField>

    <ResponseField name="name" type="string">
      Application name.
    </ResponseField>

    <ResponseField name="description" type="string | undefined">
      Application description.
    </ResponseField>

    <ResponseField name="language" type="string">
      Programming language (`javascript`, `typescript`, `python`, `go`, `php`, `ruby`, `java`, `rust`, `bun`, `static`).
    </ResponseField>

    <ResponseField name="ram" type="integer">
      Memory allocated in MB.
    </ResponseField>

    <ResponseField name="status" type="string">
      Current status: `up` or `down`.
    </ResponseField>

    <ResponseField name="subdomain" type="string | null">
      Application subdomain.
    </ResponseField>

    <ResponseField name="custom_domain" type="string | null">
      Custom domain, if configured.
    </ResponseField>

    <ResponseField name="main_file" type="string">
      Application main file.
    </ResponseField>

    <ResponseField name="version" type="string">
      Runtime version (`recommended`, `latest`, or specific version).
    </ResponseField>

    <ResponseField name="last_snapshot" type="string | null" format="date-time">
      Date of the last snapshot.
    </ResponseField>

    <ResponseField name="offline_since" type="string | null" format="date-time">
      Date since the application is offline. `null` if online.
    </ResponseField>

    <ResponseField name="created_at" type="string" format="date-time">
      Creation date.
    </ResponseField>

    <ResponseField name="updated_at" type="string" format="date-time">
      Last update date.
    </ResponseField>

    </Expandable>
  </ResponseField>

  <ResponseField name="databases" type="APIDatabase[]">
    List of user databases.

    <Expandable title="Toggle object">

    <ResponseField name="id" type="string">
      Unique database ID (Snowflake).
    </ResponseField>

    <ResponseField name="cluster" type="integer">
      Cluster ID.
    </ResponseField>

    <ResponseField name="type" type="integer">
      Type: `1` = PostgreSQL, `2` = MongoDB, `3` = Redis, `4` = MySQL.
    </ResponseField>

    <ResponseField name="owner_plan_id" type="integer">
      The owner's plan ID.
    </ResponseField>

    <ResponseField name="name" type="string">
      Database name.
    </ResponseField>

    <ResponseField name="description" type="string">
      Database description.
    </ResponseField>

    <ResponseField name="status" type="string">
      Current status: `up` or `down`.
    </ResponseField>

    <ResponseField name="ram" type="integer">
      Memory allocated in MB.
    </ResponseField>

    <ResponseField name="host" type="string">
      Host address for connection.
    </ResponseField>

    <ResponseField name="port" type="integer">
      Connection port.
    </ResponseField>

    <ResponseField name="last_snapshot" type="string | null" format="date-time">
      Date of the last snapshot.
    </ResponseField>

    <ResponseField name="offline_since" type="string | null" format="date-time">
      Date since offline.
    </ResponseField>

    <ResponseField name="created_at" type="string" format="date-time">
      Creation date.
    </ResponseField>

    <ResponseField name="updated_at" type="string" format="date-time">
      Last update date.
    </ResponseField>

    </Expandable>
  </ResponseField>

  <ResponseField name="resource_organization" type="APIWorkspaceResourceOrganization">
    The user's personal organization. It is private to this user and includes the folders and favorites used
    to organize applications and databases. Mutation details are in
    [Personal resource organization](/api-reference/endpoint/users/resource-organization).

    <Expandable title="Toggle object">

    <ResponseField name="scope" type="string">
      Sempre `personal` nesta resposta.
    </ResponseField>

    <ResponseField name="user_id" type="string">
      ID of the user who owns this organization.
    </ResponseField>

    <ResponseField name="workspace_id" type="string | null">
      Always `null` in this personal response.
    </ResponseField>

    <ResponseField name="folders" type="APIWorkspaceResourceFolder[]">
      Personal folders. Each folder contains `id`, `name`, `color`, `position`, `resources`,
      `created_at` and `updated_at`. `color` is `neutral`, `red`, `orange`, `yellow`, `green`,
      `blue` or `purple`.
    </ResponseField>

    <ResponseField name="favorites" type="APIWorkspaceFavorite[]">
      Personal favorites. Each item contains `resource_type` (`application` or `database`),
      `resource_id`, `position` and `created_at`.
    </ResponseField>

    </Expandable>
  </ResponseField>

  <ResponseField name="connections" type="APIUserConnection[]">
    Linked OAuth accounts.

    <Expandable title="Toggle object">

    <ResponseField name="provider" type="string">
      OAuth provider (`discord`, `github`, `google`).
    </ResponseField>

    <ResponseField name="username" type="string">
      Username on the provider.
    </ResponseField>

    <ResponseField name="created_at" type="string" format="date-time">
      Binding date.
    </ResponseField>

    </Expandable>
  </ResponseField>

  </Expandable>
</ResponseField>

<ResponseExample>

```json Response
{
  "response": {
    "id": "123456789012345678",
    "email": "user@example.com",
    "name": "user",
    "plan_id": 3,
    "language": "pt-br",
    "plan": {
      "id": 3,
      "name": "pro",
      "expires_at": "2026-04-20T00:00:00.000Z",
      "duration": 30,
      "memory": {
        "limit": 4096,
        "used": 1024
      }
    },
    "applications": [
      {
        "id": "v111d4af8cd74272a284831fbae14a8v",
        "type": 2,
        "name": "My API",
        "description": "REST API with Express",
        "language": "javascript",
        "ram": 512,
        "status": "up",
        "subdomain": "my-api",
        "custom_domain": null,
        "owner_id": "123456789012345678",
        "main_file": "src/index.js",
        "version": "recommended",
        "start_command": "npm start",
        "last_snapshot": "2026-03-15T10:00:00.000Z",
        "offline_since": null,
        "created_at": "2026-01-15T15:51:47.234Z",
        "updated_at": "2026-03-19T15:06:41.449Z"
      }
    ],
    "databases": [
      {
        "id": "v41806687fe6488884a464d1dc06692v",
        "type": 1,
        "name": "my-postgres",
        "description": "PostgreSQL Database",
        "status": "up",
        "ram": 1024,
        "host": "database.example.com",
        "port": 5432,
        "owner_id": "123456789012345678",
        "last_snapshot": "2026-03-18T00:00:00.000Z",
        "offline_since": null,
        "created_at": "2026-02-01T22:00:54.785Z",
        "updated_at": "2026-03-19T22:13:38.489Z"
      }
    ],
    "connections": [
      {
        "provider": "github",
        "username": "myuser",
        "created_at": "2026-01-15T15:51:47.234Z"
      },
      {
        "provider": "discord",
        "username": "myuser#1234",
        "created_at": "2026-01-15T15:51:47.234Z"
      }
    ],
    "created_at": "2026-01-15T01:26:04.784Z",
    "updated_at": "2026-03-19T22:10:31.220Z"
  }
}
```

</ResponseExample>
