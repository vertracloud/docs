---
title: 'How to Host a Static Site on Vertra Cloud'
sidebarTitle: 'Static Sites'
description: 'Deploy a static site — HTML, CSS, JavaScript or a framework build — to Vertra Cloud, with index.html as entry point and automatic SSL.'
icon: 'file-code'
---

## Requirements

- HTML, CSS and JavaScript files
- Account on [Vertra Cloud](https://vertracloud.app)

## Project Structure

```
my-site/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
├── images/
│   └── logo.png
└── vertracloud.config  (optional)
```

## Configuration

### vertracloud.config

```ini
MAIN=index.html
SUBDOMAIN=my-site
```

## Example: Landing Page

### index.html

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Site</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header>
    <h1>Welcome to My Site</h1>
    <p>Hosted on Vertra Cloud</p>
  </header>
  <script src="js/app.js"></script>
</body>
</html>
```

## Deploy

<Tabs>
  <Tab title="Dashboard">
    1. Compress the project into a `.zip` (folders like `node_modules`, `venv` and `target` are removed automatically) or push it to GitHub.
    2. In the dashboard, click **New Project** and upload the `.zip` or paste the repository URL.
    3. In **Build**, check the runtime (**Static**), the main file (`index.html`) and the memory. Start and build commands are under **Advanced settings**.
    4. Add your environment variables in **Environment**.
    5. In **Network**, turn on **web publishing** so the app gets a `*.vertraweb.app` address (Pro plan or higher, 512 MB of memory). The app must listen on port 80.
    6. Review and deploy. Follow the output in the **Logs** tab.
  </Tab>
  <Tab title="CLI">
    ```bash
    vertra deploy
    vertra logs -f
    ```
    See [CLI](/cli) for installation and login.
  </Tab>
  <Tab title="VS Code">
    Open the folder with the Vertra Cloud extension installed and run **Deploy current project**. See [VS Code extension](/vscode-extension).
  </Tab>
</Tabs>

The full walkthrough is in [First deploy](/how-to-host).

## Compiled Frontend Frameworks

For React, Vue, Svelte or Angular projects, build locally and send only the output folder:

<Tabs>
  <Tab title="React (Vite)">
    ```bash
    npm run build
    # Compress the contents of the dist/ folder
    ```
  </Tab>
  <Tab title="Vue">
    ```bash
    npm run build
    # Compress the contents of the dist/ folder
    ```
  </Tab>
  <Tab title="Svelte">
    ```bash
    npm run build
    # Compress the contents of the build/ folder
    ```
  </Tab>
  <Tab title="Angular">
    ```bash
    ng build
    # Compress the contents of the dist/project-name/ folder
    ```
  </Tab>
</Tabs>

<Warning>
  Send only the contents of the build folder (compiled files), not the entire source code. The `index.html` file must be at the root of the ZIP.
</Warning>

## Notes

- Static sites do not need a backend server
- All routes are served by the platform's internal server
- **Automatic SSL** via Let's Encrypt
- **CDN** and automatic caching for static assets
- No support for route rewriting (SPAs with client-side routing need special configuration)
